# PostHog analytics (client-side)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=

# Contact form email (server-side, used by app/actions/contact.ts).
# Get a key at https://resend.com and verify the danih.dev domain.
RESEND_API_KEY=
# Where contact submissions are delivered. Falls back to the address in lib/email.ts.
CONTACT_TO_EMAIL=
